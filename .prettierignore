# Gerados
node_modules/
.next/
out/
coverage/
package-lock.json
next-env.d.ts

# Skills instaladas (código de terceiros, não versionado)
.agents/
skills-lock.json

# Regenerado pelo `next dev`
AGENTS.md

# Gerado pelo Supabase CLI (`npm run db:types`); a CI compara com `git diff`
src/lib/supabase/database.types.ts
# Estado local do Supabase CLI (já ignorado pelo git em supabase/.gitignore)
supabase/.temp/
