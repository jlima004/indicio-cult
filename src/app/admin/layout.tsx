import type { ReactNode } from 'react'

// Namespace reservado: o módulo admin será o dono deste layout.
export default function AdminLayout({ children }: { children: ReactNode }) {
  return children
}
