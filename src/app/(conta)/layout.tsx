import type { ReactNode } from 'react'

// Namespace reservado: o módulo identity será o dono deste layout.
export default function AccountLayout({ children }: { children: ReactNode }) {
  return children
}
